import styled from 'styled-components'

export const Btn = styled.button`
  width: 10vmax;
  padding: 0.5rem 0;
  border: none;
  background: #9758a6;
  border-radius: 20px;
  color: #ffffff;
  cursor: pointer;
  font-style: normal;
  font-weight: 500;
  font-size: 16px;
  line-height: 24px;

  &:hover {
    opacity: 0.8;
  }

  &:active {
    opacity: 0.5;
  }
`
